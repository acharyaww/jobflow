# Copy this file to .env (gitignored) and fill in your value.
# This is read by the Vercel serverless function at /api/claude.
# In production, set the same variable in your Vercel project settings.
# (No VITE_ prefix — the key is now server-side only and never reaches the browser.)

ANTHROPIC_API_KEY=your_api_key_here
